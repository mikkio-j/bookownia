import React from 'react';
import Newsletter from './Newsletter';

export default {
  title: 'Bookownia/Atoms/Newsletter',
  component: Newsletter,
};

const Template = (args) => <Newsletter {...args} />;

export const Normal = Template.bind({});
