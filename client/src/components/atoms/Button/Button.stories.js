import React from 'react';
import Button from './Button';

export default {
  title: 'Bookownia/Atoms/Button',
  component: Button,
};

const Template = (args) => <Button>sprawdź</Button>;

export const Normal = Template.bind({});
